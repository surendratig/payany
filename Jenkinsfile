pipeline {
    agent any

    
    stages {
        stage('build') {
            steps {
                echo 'build is completed'
            }
        }
         stage('test') {
            steps {
                echo 'test is completed'
            }
        }
       stage('deploy') {
            steps {
                echo 'deployment is completed'
            }
        }
    }
}
